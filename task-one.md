# short response questions 
  
1. If i collaborat with a classmate the frist thing that each of the team members needs to do is make a branch of there own. This is important because that way if someone whats to add something to the `main` with out causing a change they can `git branch <branch name>` and get the aprovel of the other teammate without having to change `main`.

2. Devalopers use branch so that they can change or fix anything from `main` without changing the orignal `main` 

3. Git is a tool use in github that helps with distrituted verison control. Github is a plce were codespace lives and a cloud based service that alows developers to store, track, manage and control changes to their code.

4. The problem with the sequence is that it not in the right order. The correct command sequence is...
    1. `git add .`
    2. `git commit -m "saving work"`
    3. `git push`